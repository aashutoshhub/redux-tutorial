
//why constant file is used ?
// it is used to define constant in one file so that it can
// be used during data collection in action file (action.js) 
// and during get the data in reducer file (reducer.js) in order to store in store (store.js)

export  const ADD_TO_CART="ADD_TO_CART";
export  const REMOVE_TO_CART="REMOVE_TO_CART";
